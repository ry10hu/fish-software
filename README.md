# fish-software

make sure that you have python installed and run `pip install -r requirements.txt` in cmd, then run the according start file (eg start.bat or start.ps1 for windows and start.sh for linux)
